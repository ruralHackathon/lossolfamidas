{% extends 'base.html.twig' %}

{% block title %}Inicio{% endblock %}

{% block body %}
    <h1>Inicio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inicio.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inicio_index') }}">back to list</a>

    <a href="{{ path('inicio_edit', {'id': inicio.id}) }}">edit</a>

    {{ include('inicio/_delete_form.html.twig') }}
{% endblock %}
