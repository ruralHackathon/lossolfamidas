{% extends 'base.html.twig' %}

{% block title %}Alojamiento index{% endblock %}

{% block body %}
    <h1>Alojamiento index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for alojamiento in alojamientos %}
            <tr>
                <td>{{ alojamiento.id }}</td>
                <td>
                    <a href="{{ path('alojamiento_show', {'id': alojamiento.id}) }}">show</a>
                    <a href="{{ path('alojamiento_edit', {'id': alojamiento.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('alojamiento_new') }}">Create new</a>
{% endblock %}
